<Window x:Name="OnionSkinOverlay_Window" x:Class="OnionSkinOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:OnionSkinOverlay"
        mc:Ignorable="d"
        Title="OnionSkinOverley" Height="450" Width="800" 
        Icon="icon.png"
        AllowsTransparency="True" 
        WindowStyle="None"
        Topmost="True"
        StateChanged="Window_StateChanged" 
        PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown">

    <Window.Effect>
        <DropShadowEffect/>
    </Window.Effect>
    <Window.Background>
        <SolidColorBrush Opacity="0" Color="White"></SolidColorBrush>
    </Window.Background>


    <Grid>
        <DockPanel Grid.Column="1" Grid.Row="1">
            <DockPanel x:Name="titleBar" Background="#FF07215E" DockPanel.Dock="Top">
                <Image x:Name="image" Height="32" Width="32" DockPanel.Dock="Left" Margin="2" RenderTransformOrigin="-16.292,0.5" Source="icon.png"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="2">

                    <!-- Minimize Button -->
                    <Border Width="24" Height="24" Margin="2" HorizontalAlignment="Right" MouseLeftButtonUp="OnMinimizeWindow" Grid.Column="2">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFD0D0D0" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Text="0" FontFamily="Webdings" Foreground="White" />
                    </Border>

                    <!-- Maximize Button -->
                    <Border Width="24" Height="24" Margin="2" HorizontalAlignment="Right" MouseLeftButtonUp="OnMaximizeWindow" Grid.Column="3">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFD0D0D0" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock x:Name="IsMaximized" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Webdings" Foreground="White">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="1" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=InternalWindowState, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Value="Maximized">
                                            <Setter Property="Text" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>

                    <!-- Close Button -->
                    <Border Width="24" Height="24" Margin="2" HorizontalAlignment="Right" MouseLeftButtonUp="OnCloseWindow" Grid.Column="4">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Red" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Text="r" FontFamily="Webdings" Foreground="White" />
                    </Border>
                </StackPanel>

                <Label MouseLeftButtonDown="OnDragMoveWindow" MouseDoubleClick="OnMaximizeWindow" Margin="8 0 0 0" FontSize="12" VerticalContentAlignment="Center" Content="{Binding Path=Title, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, FallbackValue='Main Window'}" Background="#00000000" Foreground="White" />
                <Label x:Name="label_Version" MouseLeftButtonDown="OnDragMoveWindow" MouseDoubleClick="OnMaximizeWindow" Margin="0,5,0,0" Content="Version: 0.1.1" RenderTransformOrigin="1.711,0.538" HorizontalAlignment="Left" VerticalAlignment="Top" Background="#00000000" Foreground="#FF424242"/>
                <Label MouseLeftButtonDown="OnDragMoveWindow" MouseDoubleClick="OnMaximizeWindow" Background="#00000000"/>

            </DockPanel>

            <Grid Background="#FF07215E" DockPanel.Dock="Bottom" Height="32">
                <!--Bottom Controls Area-->
                <Slider x:Name="slider_ImageTransparency" Value="0.5" TickFrequency="0.05" IsSnapToTickEnabled="True" RenderTransformOrigin="1.183,-1.467" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="200" Margin="0,0,243,7" LargeChange="0.05" Maximum="1"/>
                <Label x:Name="label_1" Content="Deckkraft:" Margin="0,0,180,3" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White"/>
                <Label x:Name="label" HorizontalContentAlignment="Left" Content="{Binding Value, ElementName=slider_ImageTransparency}" Margin="0,0,148,4" ContentStringFormat="" RenderTransformOrigin="0.529,-0.24" HorizontalAlignment="Right" Height="25" VerticalAlignment="Bottom" Foreground="White" Width="35"/>
                <Button x:Name="button" Content="Bilderordner" Margin="0,0,53,6" Click="Button_Click" Width="80" VerticalAlignment="Bottom" HorizontalAlignment="Right" RenderTransformOrigin="-0.019,0.2"/>
                <CheckBox x:Name="checkBox_AlwaysOnTop" Content="Immer im Vordergrund" HorizontalAlignment="Left" Margin="19,9,0,0" VerticalAlignment="Top" Foreground="White" Unchecked="CheckBox_AlwaysOnTop_Checked" Checked="CheckBox_AlwaysOnTop_Checked" IsChecked="True"/>
                <fa:ImageAwesome x:Name="spinner_Scanning" Width="20" Height="20" Margin="0,0,20,6" Icon="CircleOutlineNotch" Spin="true" SpinDuration="4" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Foreground="White" Visibility="Hidden" />
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="100" />
                </Grid.RowDefinitions>


            </Grid>
        </DockPanel>
        <Grid>
            <Grid.Resources>
                <Style TargetType="Thumb">
                    <Style.Setters>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <!-- Hier ist der mittlere Content-->
                                    <Border Background="Transparent" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="25" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="25" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="*" />
                <RowDefinition Height="25" />
            </Grid.RowDefinitions>

            <!-- Top/Left -->
            <DockPanel LastChildFill="False" Grid.Row="0" Grid.Column="0" Height="25" VerticalAlignment="Top">
                <Thumb DockPanel.Dock="Left" Width="4" Cursor="SizeNWSE" Tag="0" DragDelta="Thumb_DragDelta" />
                <Thumb DockPanel.Dock="Top" Height="4" Cursor="SizeNWSE" Tag="0" DragDelta="Thumb_DragDelta" />
            </DockPanel>

            <!-- Top/Right -->
            <DockPanel LastChildFill="False" Grid.Row="0" Grid.Column="2">
                <Thumb DockPanel.Dock="Right" Width="4" Cursor="SizeNESW" Tag="0" DragDelta="Thumb_DragDelta" />
                <Thumb DockPanel.Dock="Top" Height="4" Cursor="SizeNESW" Tag="0" DragDelta="Thumb_DragDelta" />
            </DockPanel>

            <!-- Bottom/Left -->
            <DockPanel LastChildFill="False" Grid.Row="2" Grid.Column="0">
                <Thumb DockPanel.Dock="Left" Width="4" Cursor="SizeNESW" Tag="1" DragDelta="Thumb_DragDelta" />
                <Thumb DockPanel.Dock="Bottom" Height="4" Cursor="SizeNESW" Tag="1" DragDelta="Thumb_DragDelta" />
            </DockPanel>

            <!-- Bottom/Right -->
            <DockPanel LastChildFill="False" Grid.Row="2" Grid.Column="2">
                <Thumb DockPanel.Dock="Right" Width="4" Cursor="SizeNWSE" Tag="1" DragDelta="Thumb_DragDelta" />
                <Thumb DockPanel.Dock="Bottom" Height="4" Cursor="SizeNWSE" Tag="1" DragDelta="Thumb_DragDelta" />
            </DockPanel>

            <!-- Left -->
            <Thumb Grid.Row="1" Grid.Column="0" Width="4" Cursor="SizeWE" Tag="0" HorizontalAlignment="Left" DragDelta="Thumb_DragDelta" />

            <!-- Top -->
            <Thumb Grid.Row="0" Grid.Column="1" Height="4" Cursor="SizeNS" Tag="0" VerticalAlignment="Top" DragDelta="Thumb_DragDelta" />

            <!-- Right -->
            <Thumb Grid.Row="1" Grid.Column="2" Width="4" Cursor="SizeWE" Tag="1" HorizontalAlignment="Right" DragDelta="Thumb_DragDelta" />

            <!-- Bottom -->
            <Thumb Grid.Row="2" Grid.Column="1" Height="4" Cursor="SizeNS" Tag="1" VerticalAlignment="Bottom" DragDelta="Thumb_DragDelta" />

            <!--Mittle-->
            <Image x:Name="image_OnionLayer" Margin="0,10" Grid.Row="1" Grid.ColumnSpan="3" Opacity="{Binding Value, ElementName=slider_ImageTransparency}"/>

        </Grid>
    </Grid>

</Window>
